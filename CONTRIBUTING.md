# Contributing

Use PHP 8.5 or newer, install dependencies with `composer install`, and run
`composer check` plus `composer artifacts:verify` before submitting changes.
Changes to rules require a new profile ID. Changes to curated data require an
updated source inventory and regenerated artifacts.
