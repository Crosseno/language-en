# Repository guidance

- Keep English normalization, tokenization, equivalence, dialect, and eligibility rules explicit and versioned.
- Preserve deterministic artifacts and keep the curated source inventory synchronized with compiler configuration.
- Do not treat the MIT code license as applying to lexical data.
- Run `composer check` and `composer artifacts:verify` before handoff.
